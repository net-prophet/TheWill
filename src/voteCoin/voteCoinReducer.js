const initialState = {};

const voteCoinReducer = (state = initialState, action) => {
  //   if (action.type === "HANDLE_WEIGHT") {
  //     console.log(state);
  //     return {
  //       ...state,
  //       weighted: action.payload,
  //       address: action.payload
  //     };
  //   }
  //
  // return state;
};

export default voteCoinReducer;
